import React from 'react';
// import Explore from "../Explore"

//C&P placeholder

const Win = props => (
  <div className="Win">
    YOU WIN!
    <button onClick={props.close}>Close</button>
    </div>
)

export default Win;